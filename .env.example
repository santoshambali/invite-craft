# API Configuration
# Copy this file to .env.local or .env and update with actual values

# API Base URL - This will be injected at runtime when using Docker
# For local development: http://localhost:8080
# For Docker Compose: Set this to your backend service URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Optional: Separate invitation API URL (if different from main API)
# NEXT_PUBLIC_INVITATION_API_BASE_URL=http://localhost:8080

